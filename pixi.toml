[project]
authors = ["Daniel Cain <daniel.cain@pnnl.gov>"]
channels = ["conda-forge"]
description = "Add a short description here"
name = "cppapi"
platforms = ["linux-64"]
version = "0.1.0"

[tasks]

[dependencies]
nanobind = ">=2.7.0,<3"
python = "3.12.*"
